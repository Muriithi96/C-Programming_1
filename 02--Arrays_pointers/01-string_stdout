#include "main.h"
/**
 * prints a string,
 * followed by a new line, to stdout.
 * Return: no return
 */

void _puts(char *str);
{
     while (*str != '\0')
     {
         _putchar(*str);
	 str++;
     }
     _putchar('\n')
}

